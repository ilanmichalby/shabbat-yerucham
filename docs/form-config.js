/* ---------------------------------------------------------------------------
 * כתובת טופס ההרשמה לבודקים.
 *
 * Google Forms: פותחים את הטופס ← "שליחה" (Send) ← לשונית <> ← מעתיקים
 * את הכתובת מתוך ה-iframe. היא נראית כך:
 *   https://docs.google.com/forms/d/e/1FAIpQLSc.../viewform?embedded=true
 *
 * חשוב: זו הכתובת עם ה-/d/e/ — הכתובת הארוכה מלשונית השיתוף, ולא זו שמופיעה
 * בשורת הכתובת בזמן העריכה (אותה כתובת עובדת רק עבורכם).
 *
 * כל עוד המשתנה ריק, כפתור ההרשמה נופל בחזרה למייל מוכן מראש.
 * ------------------------------------------------------------------------- */
window.SY_TESTER_FORM_URL = '';
