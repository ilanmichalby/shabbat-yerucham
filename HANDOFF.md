# סיכום מצב — הגדרות תצוגת זמנים (2026-09-02)

## המטרה של המשתמש
להציג את **השקיעה** כזמן נפרד ובולט (כמו הדלקת נרות וצאת שבת), עם **כפתור הגדרות**
שמאפשר להדליק/לכבות זמנים. המשתמש אמר במפורש: הדלקת נרות לא מעניינת אותו, רק שקיעה,
"וכרגע זה פצפון" — כלומר השקיעה הייתה שורה קטנה בתחתית כרטיס אחר.

## שים לב: יש שתי אפליקציות בריפו
1. `mobile/App.tsx` — אפליקציית Expo / React Native.
2. `docs/` — PWA שמתפרסמת ב-GitHub Pages (`ilanmichalby.github.io`). **זו האפליקציה
   שהמשתמש בפועל פותח.** אני פספסתי את זה בהתחלה ומימשתי רק ב-mobile.

## מה כבר נעשה ומוזג ל-main
- **PR #6** (`0a22aae`) — הגדרות תצוגה ב-`mobile/App.tsx`: כפתור הפעמון הפך ל"הגדרות"
  עם שני חלקים (תצוגת זמנים / התראות), בחירת "הזמן המרכזי" מתוך 4 זמנים (מקבל כרטיס
  44pt מודגש, השאר 30pt), ומתגי הצג/הסתר. נשמר ב-AsyncStorage תחת `display-prefs-v1`.
  **לא נבדק על מכשיר, ו-`node_modules` לא מותקן כאן ולכן `tsc` לא רץ באמת.**
- **PR #7** (`dbb34f6`) — אותה יכולת ב-PWA: כפתור גלגל שיניים בכותרת, גיליון "הגדרות
  תצוגה", כרטיסי הזמנים מרונדרים ב-JS מתוך ההעדפות, שמירה ב-localStorage תחת
  `sy:display-prefs-v1`. הועלה `VERSION` ב-`docs/sw.js` מ-v11 ל-v12.
- **PR #8** (`e929cb5`) — תיקון רגרסיה מ-#7: מרקאפ `#settings-backdrop` היה **אחרי**
  הסקריפט הפנימי, ולכן `getElementById('settings-close')` היה null, נזרק TypeError,
  ו-`loadData()` לא רץ — הדף נתקע על "טוען..." בלי כרטיסים. הבלוק הועבר מעל ה-`<script>`.

## מצב אחרון
`main` = `e929cb5`. אין שינויים לא-מקומיטים. הענף המקומי הוא `claude/fix-pwa-settings-crash`
(כבר מוזג — לעבודה חדשה להסתעף מ-main מחדש).

## איך בודקים את ה-PWA כאן (עובד, השתמש בזה!)
Tailwind ו-raw.githubusercontent חסומים בסנדבוקס, אז צריך stub ל-JSON:
```
cd docs && python3 -m http.server 8899 &
# playwright מותקן ב-scratchpad; חובה executablePath:
# '/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell'
# (הבינארי הרגיל chromium-1194 נכשל על "Old Headless mode has been removed")
# הסקריפט המוכן: <scratchpad>/shot.js  — מרנדר, מדפיס כרטיסים, מצלם main.png ו-settings.png
```
בהרצה האחרונה: hero = "ניצבים · 05/09/2026", 4 כרטיסים מרונדרים כראוי.

## מה נשאר / סיכונים ידועים
1. **המשתמש עדיין לא ראה גרסה תקינה.** הוא שלח צילום מסך של הדף השבור ואמר "נראה זוועה".
   התיקון (#8) מוזג אחרי זה אבל **הוא טרם אישר שזה נראה טוב עכשיו** — זו נקודת ההמשך הראשונה.
   צריך לוודא איתו: GitHub Pages צריך דקה־שתיים לדפלוי, ואז רענון חזק / סגירה ופתיחה של
   ה-PWA פעמיים כדי ש-service worker v12 יחליף את v11 במטמון.
2. **העיצוב עצמו לא נבדק ויזואלית מול הטעם שלו** — הכרטיס המרכזי הוא `md:col-span-2`
   בגודל 48px והשאר 32px. יכול להיות שהוא ירצה שהשקיעה תהיה ברירת המחדל המרכזית,
   או סידור אחר. שווה לשאול לפני שינוי.
3. **`mobile/App.tsx` לא נבדק בכלל בריצה** — רק אימות פרסינג ב-esbuild.
4. הכרטיס המשני מציג `--:--` אם הלוח לא מכיל את הזמן (`sunset`/`rabbeinuTam` לא תמיד קיימים).

## לקח לסוכן הבא
אל תמזג שינוי ל-PWA בלי לפתוח את הדף ב-Chromium קודם. שתי הפעמים שנשברו כאן היו
נמנעות בהרצה אחת של shot.js.
